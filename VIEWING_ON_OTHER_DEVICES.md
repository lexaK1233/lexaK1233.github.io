# Как посмотреть приложение на другом устройстве

## Вариант 1: Локальная сеть (для разработки)

Если вы запускаете приложение локально на своем компьютере, другие устройства в той же сети (Wi-Fi) могут получить к нему доступ:

### Шаги:

1. **Запустите приложение** на вашем компьютере:
   ```bash
   npm run dev
   ```

2. **Узнайте IP-адрес вашего компьютера**:
   
   **macOS/Linux:**
   ```bash
   ifconfig | grep inet
   # Ищите строку вида: inet 192.168.1.5
   ```
   
   **Windows:**
   ```bash
   ipconfig
   # Ищите IPv4 Address: 192.168.1.5
   ```

3. **Настройте Vite для доступа из сети**:
   
   Обновите `vite.config.ts`:
   ```typescript
   export default defineConfig({
     server: {
       host: true, // или '0.0.0.0'
       port: 5173,
     },
     // ... остальная конфигурация
   });
   ```

4. **Откройте на другом устройстве**:
   ```
   http://192.168.1.5:5173
   ```
   (Замените `192.168.1.5` на ваш реальный IP-адрес)

---

## Вариант 2: Деплой в интернет (рекомендуется)

Разверните приложение на одной из платформ хостинга, чтобы получить постоянный URL, доступный из любой точки мира.

### А. Vercel (Рекомендуется - быстро и бесплатно)

1. **Установите Vercel CLI**:
   ```bash
   npm i -g vercel
   ```

2. **Залогиньтесь**:
   ```bash
   vercel login
   ```

3. **Деплой**:
   ```bash
   vercel
   ```
   
   Следуйте инструкциям на экране. Vercel автоматически даст вам URL вида: `https://domovoy-xxx.vercel.app`

### Б. Railway

1. Зарегистрируйтесь на [Railway.app](https://railway.app)
2. Создайте новый проект из GitHub репозитория
3. Railway автоматически определит React Router и развернет приложение
4. Получите URL вида: `https://domovoy.up.railway.app`

### В. Render

1. Зарегистрируйтесь на [Render.com](https://render.com)
2. Создайте новый "Web Service"
3. Подключите ваш GitHub репозиторий
4. Настройки:
   - Build Command: `npm install && npm run build`
   - Start Command: `npm start`
5. Получите URL вида: `https://domovoy.onrender.com`

### Г. Fly.io

1. **Установите Fly CLI**:
   ```bash
   curl -L https://fly.io/install.sh | sh
   ```

2. **Залогиньтесь**:
   ```bash
   fly auth login
   ```

3. **Создайте приложение**:
   ```bash
   fly launch
   ```

4. **Деплой**:
   ```bash
   fly deploy
   ```

---

## Вариант 3: Туннелирование (для быстрого тестирования)

Создайте временный туннель для доступа к локальному приложению через интернет:

### А. ngrok (бесплатно)

1. **Установите ngrok**: [https://ngrok.com/download](https://ngrok.com/download)

2. **Запустите приложение**:
   ```bash
   npm run dev
   ```

3. **Создайте туннель**:
   ```bash
   ngrok http 5173
   ```

4. **Получите URL** вида: `https://abc123.ngrok.io`

### Б. Cloudflare Tunnel (бесплатно)

1. **Установите cloudflared**:
   ```bash
   npm install -g cloudflared
   ```

2. **Запустите туннель**:
   ```bash
   cloudflared tunnel --url http://localhost:5173
   ```

3. **Получите URL** для доступа

---

## Настройка для продакшена

### База данных

Для продакшена замените in-memory хранилище на реальную БД:

**PostgreSQL (Рекомендуется):**
- [Supabase](https://supabase.com) - бесплатный tier
- [Neon](https://neon.tech) - бесплатный tier
- [Railway PostgreSQL](https://railway.app)

**MongoDB:**
- [MongoDB Atlas](https://mongodb.com/atlas) - бесплатный tier

### Переменные окружения

Не забудьте настроить переменные окружения на платформе хостинга:

```env
SESSION_SECRET=your-super-secret-key
DATABASE_URL=postgresql://...
CLOUDINARY_CLOUD_NAME=your-cloud-name
CLOUDINARY_API_KEY=your-api-key
CLOUDINARY_API_SECRET=your-api-secret
```

### Хранилище файлов

Для загрузки фотографий используйте облачное хранилище:
- [Cloudinary](https://cloudinary.com) - бесплатный tier 25GB
- [AWS S3](https://aws.amazon.com/s3/)
- [Vercel Blob Storage](https://vercel.com/docs/storage/vercel-blob)

---

## Безопасность

⚠️ **Важно перед продакшн-деплоем:**

1. **Измените SESSION_SECRET** на случайную строку:
   ```bash
   node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
   ```

2. **Используйте HTTPS** (все современные платформы предоставляют автоматически)

3. **Настройте реальное хеширование паролей**:
   ```bash
   npm install bcryptjs
   ```

4. **Ограничьте размер загружаемых файлов** в настройках

---

## Поддержка

Если у вас возникли проблемы:

1. Проверьте логи деплоя
2. Убедитесь, что все переменные окружения настроены
3. Проверьте, что порт правильно настроен (обычно платформы используют переменную `PORT`)

Успешного деплоя! 🚀
